# Example environment variables for the frontend
# Copy to `.env.local` or `.env.development` and update values.

# URL of the backend API used by the frontend during development
NEXT_PUBLIC_API_URL=http://localhost:3001

# Typical Node env
NODE_ENV=development

# Port Next.js will run on (dev)
PORT=3000
